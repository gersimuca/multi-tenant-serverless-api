{
  "name": "local-lambda",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && node zip.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "archiver": "^7.0.1"
  }
}
